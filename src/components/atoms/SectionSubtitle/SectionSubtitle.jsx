import React from "react";
import { StyledSubtitle } from "./SectionSubtitle.styles";

const SectionSubtitle = ({ children }) => {
  return <StyledSubtitle>{children}</StyledSubtitle>;
};

export default SectionSubtitle;
